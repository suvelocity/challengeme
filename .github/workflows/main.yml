name: Manually triggered workflow
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Person to greet'
        required: true
        default: 'Mona the Octocat'
      home:
        description: 'location'
        required: false

jobs:
  say_hello:
    runs-on: ubuntu-latest
    name: ${{ github.event.inputs.name }}
    steps:
    - name: a1a1
      run: npm i
      working-directory: ./first/
    - name: b1b1b1
      run: clone https://github.com/Cyber4sPopo/solutionme ./app
      working-directory: ./first/
    - name: c2c2c2c2
      run: npm run test
      working-directory: ./first/
        
    
